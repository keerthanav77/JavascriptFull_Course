'use strict';

let user = 'John';

use = 'Karthi'; //use variable is the wrong
console.log(user);

const interface = 'car'; //interface is the wrong ..we don't use that
const private = 'Jack';  

